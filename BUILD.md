# Build
## Requirements
 - MSVC compiler (bundled with Visual Studio)
 - Windows
## Instructions
 1. Clone the repository to your workspace.
 2. Open a Developer Command Prompt in the project directory and run:
    ```cl acca.cpp user32.lib```
 3. Locate the resulting binary, `acca.exe`.
    
